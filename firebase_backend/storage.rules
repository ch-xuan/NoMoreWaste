rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /verification_docs/{uid}/{role}/{fileName} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}

